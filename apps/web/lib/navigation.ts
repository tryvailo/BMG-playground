/**
 * Navigation wrappers from next-intl that preserve locale in URLs.
 * Use these instead of next/navigation to ensure locale is maintained.
 */
export { Link, redirect, usePathname, useRouter } from '~/i18n/routing';

