# 📋 Руководство по применению миграций

Это руководство объясняет, как применять миграции базы данных для **локального** и **продакшн** окружений.

## 🎯 Два окружения

### 1. Локальное окружение (Development)
- **Supabase**: Локальный через Docker
- **URL**: http://localhost:54321
- **Studio**: http://localhost:54323
- **База данных**: PostgreSQL на порту 54322

### 2. Продакшн окружение (Production)
- **Supabase**: Удаленный проект на supabase.com
- **URL**: Ваш проект на https://app.supabase.com/
- **База данных**: Удаленная PostgreSQL

---

## 🏠 Локальное окружение

### Способ 1: Автоматический (рекомендуется)

```bash
cd apps/web
./scripts/apply-migrations-local.sh
```

Этот скрипт:
1. Проверит, запущен ли Supabase
2. Запустит Supabase, если нужно
3. Применит все миграции через `supabase db reset`

### Способ 2: Через команды npm

```bash
cd apps/web

# Запустить Supabase (если не запущен)
pnpm supabase:start

# Применить все миграции
pnpm supabase:reset
```

### Способ 3: Вручную через Supabase Studio

Если автоматические способы не работают:

1. Откройте **Supabase Studio**: http://localhost:54323
2. Перейдите в **SQL Editor** (в левом меню)
3. Примените миграции в следующем порядке:

   **a) Базовая схема:**
   - Файл: `supabase/migrations/20241219010757_schema.sql`
   - Скопируйте весь SQL код
   - Вставьте в SQL Editor
   - Нажмите **Run** (Cmd/Ctrl + Enter)

   **b) Таблица projects:**
   - Файл: `supabase/migrations/20251129_add_ai_visibility.sql`
   - Скопируйте весь SQL код
   - Вставьте в SQL Editor
   - Нажмите **Run**

   **c) Функция ensure_user_has_project:**
   - Файл: `supabase/migrations/20251203_create_user_project.sql`
   - Скопируйте весь SQL код
   - Вставьте в SQL Editor
   - Нажмите **Run**

### Проверка локальных миграций

Выполните в SQL Editor Supabase Studio:

```sql
-- Проверка таблицы projects
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
  AND table_name = 'projects';

-- Проверка функции ensure_user_has_project
SELECT routine_name 
FROM information_schema.routines 
WHERE routine_schema = 'kit' 
  AND routine_name = 'ensure_user_has_project';
```

---

## 🌐 Продакшн окружение

### Способ 1: Автоматический через CLI (рекомендуется)

**Важно:** Перед применением миграций на продакшн убедитесь, что:
- ✅ Код протестирован локально
- ✅ Миграции применены локально и работают
- ✅ У вас есть доступ к продакшн проекту

```bash
cd apps/web

# 1. Установите PROJECT_REF из Supabase Dashboard
# Settings → General → Reference ID
export SUPABASE_PROJECT_REF=your-project-ref

# 2. Запустите скрипт
./scripts/apply-migrations-production.sh
```

Или вручную:

```bash
cd apps/web

# Связать проект (первый раз)
export SUPABASE_PROJECT_REF=your-project-ref
pnpm supabase link --project-ref $SUPABASE_PROJECT_REF

# Применить миграции
pnpm supabase db push
```

### Способ 2: Вручную через Supabase Dashboard (надежнее)

**Рекомендуется для критических миграций:**

1. Откройте **Supabase Dashboard**: https://app.supabase.com/
2. Выберите ваш **продакшн проект**
3. Перейдите в **SQL Editor** (в левом меню)
4. Примените миграции в следующем порядке:

   **a) Базовая схема:**
   - Файл: `supabase/migrations/20241219010757_schema.sql`
   - Скопируйте весь SQL код
   - Вставьте в SQL Editor
   - Нажмите **Run**

   **b) Таблица projects:**
   - Файл: `supabase/migrations/20251129_add_ai_visibility.sql`
   - Скопируйте весь SQL код
   - Вставьте в SQL Editor
   - Нажмите **Run**

   **c) Функция ensure_user_has_project:**
   - Файл: `supabase/migrations/20251203_create_user_project.sql`
   - Скопируйте весь SQL код
   - Вставьте в SQL Editor
   - Нажмите **Run**

### Проверка продакшн миграций

Выполните в SQL Editor Supabase Dashboard:

```sql
-- Проверка таблицы projects
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
  AND table_name = 'projects';

-- Проверка функции ensure_user_has_project
SELECT routine_name 
FROM information_schema.routines 
WHERE routine_schema = 'kit' 
  AND routine_name = 'ensure_user_has_project';
```

---

## 📝 Важные миграции для онбординга

Для работы онбординга необходимы следующие миграции:

1. **20241219010757_schema.sql**
   - Создает схему `kit`
   - Создает таблицу `accounts`
   - Без этой миграции ничего не будет работать

2. **20251129_add_ai_visibility.sql**
   - Создает таблицу `projects`
   - Создает таблицу `weekly_stats`
   - Без этой миграции онбординг не сможет создать проект

3. **20251203_create_user_project.sql**
   - Создает функцию `ensure_user_has_project`
   - Используется как fallback при ошибках кэша схемы

---

## ⚠️ Порядок действий при деплое

**ВАЖНО:** При деплое новых изменений:

1. ✅ **Сначала** примените миграции на продакшн
2. ✅ **Потом** задеплойте код на Vercel/хостинг

Если сделать наоборот, код может сломаться, так как таблицы еще не существуют.

---

## 🔧 Устранение проблем

### Проблема: "Could not find the table 'public.projects' in the schema cache"

**Решение:**
1. Убедитесь, что миграции применены (см. проверку выше)
2. Если миграции применены, но ошибка остается:
   - Перезапустите Supabase: `pnpm supabase:stop && pnpm supabase:start`
   - Очистите кэш браузера (Hard Refresh: Cmd+Shift+R)

### Проблема: "supabase start is not running"

**Решение:**
- Контейнеры Docker могут быть запущены вручную
- Примените миграции вручную через Supabase Studio (см. Способ 3 для локального окружения)

### Проблема: Миграции не применяются на продакшн

**Решение:**
- Используйте ручной способ через Supabase Dashboard (надежнее)
- Проверьте, что у вас есть права на проект
- Убедитесь, что PROJECT_REF правильный

---

## 📚 Дополнительные ресурсы

- [Supabase CLI Documentation](https://supabase.com/docs/guides/cli)
- [Supabase Migrations Guide](https://supabase.com/docs/guides/cli/local-development#database-migrations)
- Локальная документация: `docs/apply-migrations-manually.md`



