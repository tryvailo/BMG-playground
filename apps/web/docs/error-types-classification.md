# Классификация типов ошибок в проекте

## 📊 Общая статистика

На основе анализа всех ошибок, возникавших в проекте, выделены следующие категории:

## 🔴 Категория 1: TypeScript ошибки компиляции

### 1.1. `Cannot find name 'error'`
**Описание:** Переменная `error` используется в `catch {}` блоке, но не объявлена.

**Причина:** Замена `catch (error)` на `catch {}` без проверки использования `error` внутри блока.

**Пример:**
```typescript
// ❌ Ошибка
} catch {
  console.error('Error:', error); // error не определен
}

// ✅ Решение
} catch (error) {
  console.error('Error:', error);
}
```

**Частота:** Высокая (встречалась в ~60 файлах)

---

### 1.2. `Type 'X' is not assignable to type 'Y'`
**Описание:** Несоответствие типов при присваивании.

**Причины:**
- Неправильная типизация
- Использование `any` вместо конкретных типов
- Несоответствие типов в props компонентов

**Пример:**
```typescript
// ❌ Ошибка
const value: string = 123; // Type 'number' is not assignable to type 'string'

// ✅ Решение
const value: string = '123';
```

**Частота:** Средняя

---

### 1.3. `Object is possibly 'undefined'`
**Описание:** Доступ к свойству объекта, который может быть `undefined`.

**Причина:** Отсутствие проверок на `null`/`undefined`.

**Пример:**
```typescript
// ❌ Ошибка
const name = user.name; // user может быть undefined

// ✅ Решение
const name = user?.name || '';
```

**Частота:** Очень высокая (встречалась в ~100+ местах)

---

### 1.4. `Property 'X' does not exist on type 'Y'`
**Описание:** Обращение к несуществующему свойству.

**Причина:** Неправильное имя свойства или несоответствие интерфейса.

**Пример:**
```typescript
// ❌ Ошибка
audit.schema.hasFAQ // Property 'hasFAQ' does not exist

// ✅ Решение
audit.schema.hasFAQPage // Правильное имя свойства
```

**Частота:** Средняя

---

## 🟡 Категория 2: ESLint ошибки

### 2.1. `@typescript-eslint/no-unused-vars` - Неиспользуемые переменные

#### 2.1.1. Неиспользуемые импорты
**Описание:** Импортированные модули/компоненты не используются в коде.

**Примеры:**
- `ChevronRight`, `Info`, `AlertCircle`, `TrendingUp`, `Lightbulb`, `Tooltip` и др.

**Причины:**
- Копирование кода с лишними импортами
- Рефакторинг без очистки импортов
- Автодополнение IDE

**Частота:** Очень высокая (~200+ ошибок)

**Решение:**
```typescript
// ❌ Ошибка
import { ChevronRight, Info, AlertCircle } from 'lucide-react';

// ✅ Решение - удалить неиспользуемые
import { Info } from 'lucide-react';
```

---

#### 2.1.2. Неиспользуемые переменные в catch блоках
**Описание:** Переменная `error` в `catch (error)` не используется.

**Причина:** Обработка ошибок без логирования или использования переменной.

**Частота:** Высокая (~50+ ошибок)

**Решение:**
```typescript
// ❌ Ошибка
} catch (error) {
  return false; // error не используется
}

// ✅ Решение
} catch (_error) {
  return false; // префикс _ указывает на намеренное неиспользование
}
```

**Настройка ESLint:**
```javascript
'@typescript-eslint/no-unused-vars': [
  'error',
  {
    caughtErrorsIgnorePattern: '^_', // Разрешить _error
  },
]
```

---

#### 2.1.3. Неиспользуемые параметры функций
**Описание:** Параметры функций объявлены, но не используются.

**Пример:**
```typescript
// ❌ Ошибка
function processData(data: Data, index: number) {
  return data.value; // index не используется
}

// ✅ Решение
function processData(data: Data, _index: number) {
  return data.value;
}
```

**Частота:** Средняя

---

### 2.2. `@typescript-eslint/no-explicit-any` - Использование типа `any`
**Описание:** Использование типа `any` вместо конкретных типов.

**Причины:**
- Быстрое прототипирование
- Незнание правильных типов
- Сложные типы

**Частота:** Высокая (~76 ошибок)

**Пример:**
```typescript
// ❌ Ошибка
const BentoCard = ({ children, className, title, subtitle }: any) => {

// ✅ Решение
interface BentoCardProps {
  children?: React.ReactNode;
  className?: string;
  title?: string;
  subtitle?: string;
}
const BentoCard = ({ children, className, title, subtitle }: BentoCardProps) => {
```

---

### 2.3. `react/no-unescaped-entities` - Неэкранированные символы в JSX
**Описание:** Использование специальных символов (`'`, `"`) в JSX без экранирования.

**Причины:**
- Прямой ввод текста в JSX
- Копирование текста из других источников

**Частота:** Высокая (~67 ошибок)

**Пример:**
```typescript
// ❌ Ошибка
<p>It's a beautiful day</p>
<p>"Hello world"</p>

// ✅ Решение
<p>It&apos;s a beautiful day</p>
<p>&quot;Hello world&quot;</p>
```

**Символы для замены:**
- `'` → `&apos;` или `&#39;`
- `"` → `&quot;` или `&#34;`

---

### 2.4. `no-useless-escape` - Ненужные экранирования
**Описание:** Лишние обратные слеши в регулярных выражениях.

**Пример:**
```typescript
// ❌ Ошибка
const pattern = /\.\/\//; // Ненужные экранирования

// ✅ Решение
const pattern = /\.\//; // Правильное экранирование
```

**Частота:** Низкая (~23 ошибки)

---

### 2.5. React Hook зависимости
**Описание:** Отсутствие зависимостей в массиве зависимостей хуков.

**Пример:**
```typescript
// ❌ Ошибка
const loadData = useCallback(() => {
  fetchData(result); // result используется, но не в зависимостях
}, []);

// ✅ Решение
const loadData = useCallback(() => {
  fetchData(result);
}, [result]); // Добавить result в зависимости
```

**Частота:** Средняя

---

## 🟢 Категория 3: Логические ошибки

### 3.1. Неправильное использование состояния
**Описание:** Использование устаревшего значения состояния в setState callback.

**Пример:**
```typescript
// ❌ Ошибка
setAuditDate(result.createdAt); // result может быть устаревшим

// ✅ Решение
setAuditDate((prevDate) => {
  if (latestAudit?.createdAt) {
    return latestAudit.createdAt;
  }
  return prevDate;
});
```

**Частота:** Низкая

---

### 3.2. Утечки памяти (Memory Leaks)
**Описание:** Неочищенные таймеры и подписки.

**Пример:**
```typescript
// ❌ Ошибка
useEffect(() => {
  setTimeout(() => {
    // действие
  }, 1000);
  // Таймер не очищается
}, []);

// ✅ Решение
useEffect(() => {
  const timeoutId = setTimeout(() => {
    // действие
  }, 1000);
  
  return () => clearTimeout(timeoutId);
}, []);
```

**Частота:** Низкая

---

## 📈 Статистика по категориям

### По частоте возникновения:
1. **Неиспользуемые импорты** - ~200+ ошибок (40%)
2. **Неиспользуемые переменные в catch** - ~50+ ошибок (10%)
3. **Типы any** - ~76 ошибок (15%)
4. **Неэкранированные символы** - ~67 ошибок (13%)
5. **Object is possibly 'undefined'** - ~100+ ошибок (20%)
6. **Другие** - ~50+ ошибок (10%)

### По критичности:
1. **Критичные (TypeScript):** Блокируют компиляцию
   - `Cannot find name 'error'`
   - `Type 'X' is not assignable to type 'Y'`
   - `Property 'X' does not exist`

2. **Важные (ESLint):** Нарушают стандарты кода
   - `@typescript-eslint/no-unused-vars`
   - `@typescript-eslint/no-explicit-any`
   - `react/no-unescaped-entities`

3. **Предупреждения:** Не критичны, но желательно исправить
   - `no-useless-escape`
   - React Hook зависимости

---

## 🛠️ Решения по категориям

### Автоматические исправления:
1. **ESLint --fix** - исправляет:
   - Неэкранированные символы (частично)
   - Ненужные экранирования
   - Форматирование

2. **TypeScript компилятор** - помогает найти:
   - Ошибки типов
   - Неопределенные переменные
   - Несуществующие свойства

### Ручные исправления требуются для:
1. Неиспользуемых импортов
2. Типов `any` (нужно определить правильные типы)
3. Проверок на `undefined`/`null`
4. Исправления логики состояния

---

## 📝 Рекомендации по предотвращению

### 1. Настройка IDE
```json
{
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": true,
    "source.organizeImports": true
  }
}
```

### 2. Pre-commit hooks
```bash
pnpm lint:fix
pnpm typecheck
```

### 3. CI/CD проверки
```yaml
- name: Lint
  run: pnpm lint
- name: Type Check
  run: pnpm typecheck
```

### 4. Правила разработки
- Всегда использовать типы вместо `any`
- Проверять `undefined`/`null` перед доступом к свойствам
- Удалять неиспользуемые импорты сразу
- Использовать `catch (_error)` если error не нужен
- Экранировать специальные символы в JSX

---

## 🔍 Идентификация ошибок

### Быстрая идентификация по сообщению:

| Паттерн сообщения | Тип ошибки | Категория |
|-------------------|------------|-----------|
| `Cannot find name` | TypeScript | Критичная |
| `is not assignable` | TypeScript | Критичная |
| `is possibly 'undefined'` | TypeScript | Критичная |
| `is defined but never used` | ESLint | Важная |
| `Unexpected any` | ESLint | Важная |
| `can be escaped` | ESLint | Важная |
| `missing dependency` | ESLint | Предупреждение |
| `no-useless-escape` | ESLint | Предупреждение |

### По файлам:
- **TypeScript ошибки** - в файлах `.ts`, `.tsx`
- **ESLint ошибки** - во всех файлах проекта
- **React ошибки** - в компонентах `.tsx`

---

## 📚 Связанные документы

- [Анализ причин ошибок](./eslint-errors-analysis.md)
- [Постоянное решение](./permanent-solution.md)
- [Итоговый отчет](./lint-fix-summary.md)







