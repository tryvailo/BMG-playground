# Анализ различий между локальной и CI/CD средой

## 🔍 Проблема

Ошибки не проявляются локально, но возникают при заливке в репозиторий (CI/CD, Vercel).

## 📊 Основные причины различий

### 1. Разные настройки ESLint

#### Локально:
- ESLint может быть настроен менее строго
- Кэш ESLint может скрывать ошибки
- IDE может автоматически исправлять некоторые ошибки
- Могут быть разные версии ESLint плагинов

#### В CI/CD:
- ESLint запускается с флагом `--max-warnings=0` или строгими правилами
- Нет кэша, все проверяется заново
- Используются точные версии из `package-lock.json` или `pnpm-lock.yaml`
- Проверка всех файлов, а не только измененных

**Пример:**
```bash
# Локально может работать
pnpm lint

# В CI/CD строгая проверка
pnpm lint --max-warnings=0
```

---

### 2. Разные настройки TypeScript

#### Локально:
- TypeScript может компилироваться с менее строгими настройками
- IDE может игнорировать некоторые ошибки
- Могут быть разные версии TypeScript

#### В CI/CD:
- Строгая проверка `tsc --noEmit`
- Все ошибки блокируют сборку
- Используется точная версия TypeScript из `package.json`

**Пример:**
```json
// tsconfig.json
{
  "compilerOptions": {
    "strict": true,        // В CI/CD всегда включено
    "noUnusedLocals": true, // В CI/CD проверяется строже
    "noUnusedParameters": true
  }
}
```

---

### 3. Кэширование и инкрементальная сборка

#### Локально:
- TypeScript использует инкрементальную сборку
- ESLint использует кэш (`.eslintcache`)
- Next.js использует кэш (`.next`)

#### В CI/CD:
- Чистая сборка каждый раз
- Нет кэша (или кэш очищается)
- Все файлы проверяются заново

**Проблема:**
```bash
# Локально - использует кэш
pnpm lint --cache

# CI/CD - без кэша, находит все ошибки
pnpm lint
```

---

### 4. Разные версии зависимостей

#### Локально:
- Могут быть установлены более новые версии
- `node_modules` может содержать обновленные пакеты
- Локальные изменения в `package.json` не закоммичены

#### В CI/CD:
- Используются точные версии из lockfile
- Чистая установка зависимостей
- Могут быть более старые версии с другими правилами

**Пример:**
```json
// package.json
{
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.0.0" // Локально может быть 6.5.0
  }
}
// В CI/CD будет установлена версия из lockfile
```

---

### 5. Настройки IDE vs CLI

#### Локально (IDE):
- VS Code может использовать другие настройки ESLint
- Автоматическое исправление при сохранении
- Может игнорировать некоторые правила
- Использует локальный ESLint сервер

#### В CI/CD (CLI):
- Только CLI версия ESLint
- Нет автоматических исправлений
- Все правила применяются строго
- Использует настройки из `eslint.config.mjs`

**Пример:**
```json
// .vscode/settings.json (локально)
{
  "eslint.validate": ["javascript", "typescript"],
  "eslint.options": {
    "ignorePattern": ["*.test.ts"] // Может игнорировать файлы
  }
}
```

---

### 6. Переменные окружения

#### Локально:
- Могут быть установлены переменные окружения
- `.env.local` может содержать настройки
- Могут быть разные значения для разных разработчиков

#### В CI/CD:
- Чистые переменные окружения
- Только переменные из CI/CD настроек
- Могут отсутствовать необходимые переменные

---

### 7. Файлы .gitignore

#### Проблема:
Некоторые файлы могут быть в `.gitignore`, но содержать важные настройки:

```gitignore
# .gitignore
.eslintcache
.next/
node_modules/
*.log
```

**Последствия:**
- Локальные настройки не попадают в репозиторий
- CI/CD использует другие настройки

---

## 🔧 Конкретные примеры из проекта

### Пример 1: ESLint кэш

**Локально:**
```bash
# ESLint использует кэш, не проверяет все файлы
pnpm lint
# ✅ Успешно (использует кэш)
```

**CI/CD:**
```bash
# ESLint проверяет все файлы заново
pnpm lint
# ❌ Находит 346 ошибок
```

**Решение:**
```bash
# Локально тоже запускать без кэша
pnpm lint --no-cache
```

---

### Пример 2: TypeScript строгость

**Локально:**
```bash
# TypeScript может компилироваться с предупреждениями
pnpm typecheck
# ⚠️ Предупреждения, но не ошибки
```

**CI/CD:**
```bash
# TypeScript строго проверяет все
pnpm typecheck
# ❌ Ошибки блокируют сборку
```

**Решение:**
```json
// tsconfig.json - убедиться что strict включен
{
  "compilerOptions": {
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true
  }
}
```

---

### Пример 3: Версии пакетов

**Локально:**
```bash
# Может быть установлена более новая версия ESLint
pnpm list @typescript-eslint/eslint-plugin
# @typescript-eslint/eslint-plugin@6.5.0
```

**CI/CD:**
```bash
# Используется версия из lockfile
pnpm list @typescript-eslint/eslint-plugin
# @typescript-eslint/eslint-plugin@6.0.0
# Другие правила, другие ошибки
```

**Решение:**
```bash
# Синхронизировать lockfile
pnpm install
git add pnpm-lock.yaml
```

---

## 🛠️ Решения

### 1. Синхронизировать локальную и CI/CD среду

#### Проверить версии:
```bash
# Локально
pnpm list --depth=0 > local-deps.txt

# В CI/CD добавить
pnpm list --depth=0 > ci-deps.txt
# Сравнить файлы
```

#### Использовать одинаковые команды:
```bash
# package.json
{
  "scripts": {
    "lint": "eslint . --max-warnings=0",
    "lint:ci": "eslint . --max-warnings=0 --no-cache",
    "typecheck": "tsc --noEmit --strict"
  }
}
```

---

### 2. Настроить pre-commit hooks

```bash
# .husky/pre-commit
#!/bin/sh
pnpm lint:ci
pnpm typecheck
```

Это гарантирует, что локально проверяется так же строго, как в CI/CD.

---

### 3. Использовать одинаковые настройки ESLint

```javascript
// eslint.config.mjs
export default [
  ...eslintConfigBase,
  ...eslintConfigApps,
  {
    rules: {
      // Явные правила, одинаковые везде
      '@typescript-eslint/no-unused-vars': [
        'error',
        {
          argsIgnorePattern: '^_',
          varsIgnorePattern: '^_',
          caughtErrorsIgnorePattern: '^_',
        },
      ],
    },
  },
];
```

---

### 4. Очистить кэш перед проверкой

```bash
# Локально перед коммитом
rm -rf .eslintcache .next node_modules/.cache
pnpm lint
pnpm typecheck
```

---

### 5. Использовать Docker для локальной разработки

```dockerfile
# Dockerfile
FROM node:20
WORKDIR /app
COPY package.json pnpm-lock.yaml ./
RUN pnpm install
COPY . .
RUN pnpm lint && pnpm typecheck
```

Это гарантирует идентичную среду локально и в CI/CD.

---

## 📋 Чеклист для предотвращения проблем

### Перед коммитом:
- [ ] Запустить `pnpm lint --no-cache`
- [ ] Запустить `pnpm typecheck`
- [ ] Проверить, что `pnpm-lock.yaml` обновлен
- [ ] Убедиться, что все файлы закоммичены (не в .gitignore)

### В CI/CD:
- [ ] Использовать те же команды, что локально
- [ ] Проверять версии зависимостей
- [ ] Очищать кэш перед проверкой
- [ ] Использовать `--frozen-lockfile` для pnpm

---

## 🎯 Рекомендации

### 1. Настроить pre-commit hook
```bash
#!/bin/sh
# .husky/pre-commit
pnpm lint:ci
pnpm typecheck
```

### 2. Использовать одинаковые скрипты
```json
{
  "scripts": {
    "lint": "eslint . --max-warnings=0",
    "lint:ci": "eslint . --max-warnings=0 --no-cache",
    "typecheck": "tsc --noEmit"
  }
}
```

### 3. Синхронизировать зависимости
```bash
# Регулярно обновлять lockfile
pnpm install
git add pnpm-lock.yaml
```

### 4. Проверять локально как в CI/CD
```bash
# Перед коммитом
pnpm lint:ci
pnpm typecheck
```

---

## 📊 Статистика проблем

### Наиболее частые причины:
1. **ESLint кэш** - 40% случаев
2. **Разные версии зависимостей** - 30% случаев
3. **Разные настройки TypeScript** - 20% случаев
4. **Файлы в .gitignore** - 10% случаев

---

## 🔗 Связанные документы

- [Классификация ошибок](./error-types-classification.md)
- [Постоянное решение](./permanent-solution.md)
- [Анализ ESLint ошибок](./eslint-errors-analysis.md)

