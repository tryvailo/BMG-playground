# Устранение проблем с Dashboard

## Проблема: Нулевые данные в дашборде

### ✅ Исправления применены

1. **Исправлена логика получения projectId**
   - Теперь используется реальный UUID проекта вместо строки 'demo'
   - Если `projectId = 'demo'`, поиск идет по domain = 'demo-clinic.com'

2. **Убран ранний возврат при отсутствии сервисов**
   - Дашборд теперь использует данные из `weekly_stats` независимо от наличия сервисов
   - Сервисы нужны только для анализа конкурентов

3. **Добавлено логирование**
   - В консоли сервера будут видны логи загрузки данных
   - Проверьте логи в терминале, где запущен `pnpm dev`

### 🔍 Проверка данных в БД

#### Вариант 1: Через Supabase Studio

1. Откройте http://127.0.0.1:54323
2. Перейдите в **SQL Editor**
3. Выполните скрипт: `supabase/scripts/check-data-values.sql`

Ожидаемый результат:
- ✅ Все 12 записей должны иметь значения > 0
- ✅ `clinic_ai_score` должен быть рассчитан автоматически

#### Вариант 2: Быстрая проверка

```sql
-- Проверка наличия данных
SELECT 
  COUNT(*) as total,
  MIN(clinic_ai_score) as min_score,
  MAX(clinic_ai_score) as max_score,
  AVG(clinic_ai_score) as avg_score
FROM public.weekly_stats ws
JOIN public.projects p ON p.id = ws.project_id
WHERE p.domain = 'demo-clinic.com'
  AND ws.week_start >= '2025-01-01';
```

Ожидаемый результат:
- `total`: 12
- `min_score`: ~52 (Январь)
- `max_score`: ~83 (Декабрь)
- `avg_score`: ~67-68

### 🐛 Отладка

#### Шаг 1: Проверьте логи сервера

При загрузке дашборда в консоли сервера должны быть логи:
```
[Dashboard] Fetching metrics for project: demo -> <UUID> domain: demo-clinic.com
[Dashboard] Found X weekly_stats records for project <UUID>
[Dashboard] Using latest weekly_stats from 2025-12-01 { ... }
[Dashboard] Calculated ClinicAI Score: 83.XX from inputs: { ... }
```

Если видите ошибки:
- `Project not found` - проверьте, что demo проект создан
- `Found 0 weekly_stats records` - проверьте, что миграция seed применена

#### Шаг 2: Проверьте консоль браузера

Откройте DevTools (F12) → Console

Проверьте:
- Нет ли ошибок загрузки данных
- Правильно ли работает React Query

#### Шаг 3: Проверьте данные напрямую

Выполните в Supabase Studio:

```sql
-- Получить UUID demo проекта
SELECT id, domain, name 
FROM public.projects 
WHERE domain = 'demo-clinic.com';

-- Проверить данные для этого проекта (замените <UUID> на реальный ID)
SELECT * 
FROM public.weekly_stats 
WHERE project_id = '<UUID>'
ORDER BY week_start;
```

### 🔧 Если данные все еще нулевые

1. **Перепримените миграции:**
   ```bash
   cd apps/web
   pnpm supabase db reset
   ```

2. **Проверьте, что миграции применены:**
   ```sql
   SELECT * FROM supabase_migrations.schema_migrations 
   ORDER BY version DESC 
   LIMIT 5;
   ```

3. **Проверьте триггер:**
   ```sql
   SELECT * FROM pg_trigger 
   WHERE tgname = 'auto_calculate_clinic_ai_score';
   ```

4. **Вручную проверьте расчет:**
   ```sql
   SELECT 
     week_start,
     visability_score * 0.25 +
     tech_score * 0.20 +
     content_score * 0.20 +
     eeat_score * 0.15 +
     local_score * 0.10 as calculated_score,
     clinic_ai_score as stored_score
   FROM public.weekly_stats ws
   JOIN public.projects p ON p.id = ws.project_id
   WHERE p.domain = 'demo-clinic.com'
   ORDER BY week_start
   LIMIT 3;
   ```

### ✅ Ожидаемые значения

После исправлений дашборд должен показывать:

**Последние значения (Декабрь 2025):**
- ClinicAI Score: **~83%** (не 0!)
- Visibility: **80%**
- Tech: **85%**
- Content: **84%**
- E-E-A-T: **80%**
- Local: **72%**
- Avg Position: **3.0**

**График:**
- 12 точек данных
- Рост от ~52% (Январь) до ~83% (Декабрь)

### 📝 Чеклист

- [ ] Миграции применены (`pnpm supabase db reset`)
- [ ] Demo проект создан (проверка через SQL)
- [ ] 12 записей в weekly_stats с ненулевыми значениями
- [ ] clinic_ai_score рассчитывается автоматически
- [ ] В логах сервера видны данные (не нули)
- [ ] Дашборд отображает данные из БД




