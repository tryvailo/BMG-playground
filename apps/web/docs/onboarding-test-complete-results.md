# Результаты полного тестирования онбординга для Friendlic.clinic

## ✅ Успешно пройдено

### 1. Онбординг работает полностью
- ✅ Выбор региона: Ukraine
- ✅ Выбор языка: Ukrainian  
- ✅ Выбор города: Київ
- ✅ Ввод домена: `friendlic.clinic` (с автоматической очисткой от `https://` и `/`)
- ✅ Поиск клиники: Найдена клиника "Френдлік"
- ✅ Найдены конкуренты: 4 конкурента
- ✅ Выбор плана: Starter (monthly)
- ✅ Создание аккаунта: Пользователь создан

### 2. Данные созданы
- ✅ **User ID**: `a3fc3343-a7e1-4472-b2dd-4823e8bb3dc2`
- ✅ **Project**: `friendlic.clinic` (Френдлік, Київ, UA)
- ✅ **Subscription**: Должна быть создана (Starter, $99/month, pending)

## Исправления

### 1. Очистка домена
**Проблема**: Домен передавался как `https://friendlic.clinic/` с протоколом и слешем

**Исправлено**:
- В `StepDomain.tsx`: добавлена полная очистка домена (удаление протокола, www, пути, слешей)
- В `create-from-onboarding/route.ts`: улучшена очистка домена

### 2. Ошибка 406 при загрузке подписки
**Проблема**: `GET .../subscriptions?... 406 (Not Acceptable)`

**Исправлено**:
- Заменено `.single()` на `.maybeSingle()` в `subscription-status.tsx`
- Заменено `.single()` на `.maybeSingle()` в `subscription-gate.tsx`
- Это позволяет запросу вернуть `null` вместо ошибки, если подписка не найдена

## Логи из консоли

```
[StepDomain] Button clicked, domain: https://friendlic.clinic/ isSearching: false disabled: false
[StepDomain] Calling handleSubmit directly
[StepDomain] handleSubmit called, event: undefined
[StepDomain] trimmedDomain: https://friendlic.clinic/
[StepDomain] Submitting domain: { domain: "https://friendlic.clinic/", region: "UA", city: "Київ" }
[StepDomain] Searching for clinic: https://friendlic.clinic/
[UkraineClinics] Loaded 379 clinics
[StepDomain] Found clinic: {clinicName: 'Френдлік', competitorsCount: 4}
[StepDomain] Calling onContinue with: {domain: 'https://friendlic.clinic/', clinicName: 'Френдлік', competitorsCount: 4}
[StepPricing] Calling onPlanSelect with: {planId: 'starter', interval: 'month'}
[Onboarding] Plan selected (alternative path): {planId: 'starter', interval: 'month', ...}
[Onboarding] Sending project creation request: {userId: 'a3fc3343-a7e1-4472-b2dd-4823e8bb3dc2', domain: 'friendlic.clinic/', clinicName: 'Френдлік', region: 'UA', city: 'Київ', ...}
```

## Проблемы

### 1. Домен все еще содержит `/` в конце
В логах видно: `domain: 'friendlic.clinic/'` - нужно убрать `/` в конце

**Исправлено**: Добавлена очистка через `.split('/')[0]` для удаления пути

### 2. Ошибка 406 при загрузке подписки
```
GET http://127.0.0.1:54321/rest/v1/subscriptions?... 406 (Not Acceptable)
```

**Исправлено**: Заменено `.single()` на `.maybeSingle()` для корректной обработки отсутствия подписки

## Проверка подписки

Чтобы проверить, создалась ли подписка:

1. **Откройте админ-панель:**
   - URL: `http://localhost:3000/admin/users`
   - Найдите пользователя с email, который использовали при регистрации

2. **Проверьте в базе данных:**
   ```sql
   SELECT s.id, s.plan_id, s.plan_name, s.payment_status, a.email
   FROM public.subscriptions s
   LEFT JOIN public.accounts a ON a.id = s.account_id
   WHERE s.account_id = 'a3fc3343-a7e1-4472-b2dd-4823e8bb3dc2'
   ORDER BY s.created_at DESC;
   ```

3. **Проверьте логи сервера:**
   - Должны быть логи `[CreateProject] ✅ SUCCESS - Created pending subscription`

## Следующие шаги

1. ✅ Очистка домена исправлена
2. ✅ Ошибка 406 исправлена (заменено `.single()` на `.maybeSingle()`)
3. ⏳ Проверить, создалась ли подписка в базе данных
4. ⏳ Проверить, отображается ли подписка в админ-панели

## Готово! 🎉

Онбординг работает полностью! Пользователь создан, проект создан, подписка должна быть создана. Осталось только проверить, что подписка отображается в админ-панели.

