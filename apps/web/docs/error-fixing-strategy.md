# Стратегия эффективного исправления ошибок

## 🎯 Цель

Исправить все ошибки **за минимальное количество билдов**, не откатываясь назад.

## ❌ Почему НЕ стоит откатываться

1. **Потеря прогресса** - все исправления будут потеряны
2. **Проблема вернется** - ошибки появятся снова при следующем коммите
3. **Технический долг** - лучше исправить сейчас

## ✅ Правильная стратегия

### Этап 1: Анализ и приоритизация (5 минут)

#### 1.1. Получить статистику ошибок
```bash
# Подсчитать ошибки по типам
pnpm --filter web lint 2>&1 | grep -oE "@typescript-eslint/[^ ]+|react/[^ ]+" | sort | uniq -c | sort -rn

# Найти файлы с наибольшим количеством ошибок
pnpm --filter web lint 2>&1 | grep -E "error" | cut -d: -f1 | sort | uniq -c | sort -rn | head -10
```

#### 1.2. Приоритизация
1. **Критичные (TypeScript)** - блокируют компиляцию
2. **Важные (ESLint)** - нарушают стандарты кода
3. **Предупреждения** - можно исправить позже

---

### Этап 2: Массовое автоматическое исправление (10 минут)

#### 2.1. Автоматически исправимые ошибки
```bash
# Запустить ESLint --fix (исправляет ~30-40% ошибок автоматически)
pnpm --filter web lint --fix
```

**Что исправляется автоматически:**
- Неэкранированные символы (частично)
- Ненужные экранирования
- Форматирование

#### 2.2. Массовое исправление catch блоков
```bash
# Использовать скрипт для исправления всех catch блоков
./apps/web/scripts/fix-catch-blocks.sh
```

Или вручную:
```bash
# Найти все catch {} где используется error
find apps/web -name "*.ts" -o -name "*.tsx" | xargs grep -l "catch {" | xargs grep -A 3 "catch {" | grep "error"

# Заменить на catch (error) где нужно
# Заменить на catch (_error) где не нужно
```

---

### Этап 3: Исправление по категориям (30-60 минут)

#### 3.1. Неиспользуемые импорты (самое быстрое)

**Стратегия:** Исправлять по файлам, начиная с самых проблемных

```bash
# Найти топ-10 файлов с неиспользуемыми импортами
pnpm --filter web lint 2>&1 | grep -E "error.*is defined but never used" | cut -d: -f1 | sort | uniq -c | sort -rn | head -10
```

**Алгоритм:**
1. Взять файл с наибольшим количеством ошибок
2. Удалить все неиспользуемые импорты
3. Проверить локально: `pnpm --filter web lint --no-cache | grep "имя_файла"`
4. Перейти к следующему файлу

**Пример:**
```typescript
// До
import { ChevronRight, Info, AlertCircle, TrendingUp } from 'lucide-react';

// После (удалить неиспользуемые)
import { Info } from 'lucide-react';
```

#### 3.2. Типы any (средняя сложность)

**Стратегия:** Исправлять по файлам, создавая интерфейсы

```bash
# Найти все any
pnpm --filter web lint 2>&1 | grep -E "Unexpected any" | cut -d: -f1 | sort | uniq -c | sort -rn | head -10
```

**Алгоритм:**
1. Взять файл
2. Найти все `any`
3. Создать интерфейсы для каждого
4. Заменить `any` на интерфейсы
5. Проверить локально

**Пример:**
```typescript
// До
const BentoCard = ({ children, className, title, subtitle }: any) => {

// После
interface BentoCardProps {
  children?: React.ReactNode;
  className?: string;
  title?: string;
  subtitle?: string;
}
const BentoCard = ({ children, className, title, subtitle }: BentoCardProps) => {
```

#### 3.3. Неэкранированные символы (быстро)

**Стратегия:** Массовая замена через скрипт

```bash
# Найти все файлы с неэкранированными символами
pnpm --filter web lint 2>&1 | grep -E "can be escaped" | cut -d: -f1 | sort | uniq

# Исправить автоматически (если возможно)
pnpm --filter web lint --fix
```

**Ручное исправление:**
```typescript
// До
<p>It's a beautiful day</p>

// После
<p>It&apos;s a beautiful day</p>
```

---

### Этап 4: Проверка перед коммитом (5 минут)

#### 4.1. Локальная проверка (как в CI/CD)
```bash
# Очистить кэш
rm -rf .eslintcache apps/web/.eslintcache

# Проверить без кэша
pnpm --filter web lint --no-cache

# Проверить TypeScript
pnpm --filter web typecheck
```

#### 4.2. Если ошибок много - коммитить постепенно
```bash
# Коммит 1: Автоматические исправления
git add -A
git commit -m "fix: автоматическое исправление ESLint ошибок"

# Коммит 2: Исправление catch блоков
git add -A
git commit -m "fix: исправление всех catch блоков"

# Коммит 3: Удаление неиспользуемых импортов (топ-10 файлов)
git add -A
git commit -m "fix: удаление неиспользуемых импортов в основных компонентах"

# И так далее...
```

---

## 🚀 Оптимизированный алгоритм (минимум билдов)

### Вариант 1: Один большой коммит (быстро, но рискованно)

```bash
# 1. Автоматическое исправление
pnpm --filter web lint --fix

# 2. Исправить catch блоки
./apps/web/scripts/fix-catch-blocks.sh

# 3. Исправить топ-20 файлов с ошибками вручную
# (используя список из анализа)

# 4. Проверить локально
pnpm --filter web lint --no-cache
pnpm --filter web typecheck

# 5. Если ошибок < 50 - коммитить
git add -A
git commit -m "fix: массовое исправление ESLint и TypeScript ошибок"
git push
```

**Плюсы:** Быстро, один билд  
**Минусы:** Если что-то сломается, сложно откатить

---

### Вариант 2: Поэтапные коммиты (безопасно, но больше билдов)

#### Этап 1: Автоматические исправления
```bash
pnpm --filter web lint --fix
git add -A
git commit -m "fix: автоматическое исправление ESLint ошибок"
git push
# Ждем билд, проверяем результат
```

#### Этап 2: Catch блоки
```bash
./apps/web/scripts/fix-catch-blocks.sh
git add -A
git commit -m "fix: исправление всех catch блоков"
git push
# Ждем билд
```

#### Этап 3: Неиспользуемые импорты (топ-10 файлов)
```bash
# Исправить вручную топ-10 файлов
git add -A
git commit -m "fix: удаление неиспользуемых импортов (топ-10 файлов)"
git push
# Ждем билд
```

**Плюсы:** Безопасно, легко откатить  
**Минусы:** Больше билдов

---

### Вариант 3: Гибридный подход (РЕКОМЕНДУЕТСЯ)

#### Шаг 1: Подготовка (локально, без коммита)
```bash
# 1. Автоматическое исправление
pnpm --filter web lint --fix

# 2. Исправить catch блоки
./apps/web/scripts/fix-catch-blocks.sh

# 3. Исправить топ-20 файлов с ошибками
# (используя список из анализа)

# 4. Проверить локально (как в CI/CD)
rm -rf .eslintcache apps/web/.eslintcache
pnpm --filter web lint --no-cache
pnpm --filter web typecheck
```

#### Шаг 2: Если ошибок < 100 - коммитить все сразу
```bash
git add -A
git commit -m "fix: массовое исправление ESLint и TypeScript ошибок"
git push
```

#### Шаг 3: Если ошибок > 100 - разбить на части
```bash
# Часть 1: Автоматические + catch блоки
git add apps/web/lib apps/web/app
git commit -m "fix: исправление catch блоков и автоматические исправления"
git push

# Часть 2: Компоненты (топ-10 файлов)
git add apps/web/components
git commit -m "fix: удаление неиспользуемых импортов в компонентах"
git push

# И так далее...
```

---

## 📋 Чеклист для эффективного исправления

### Перед началом:
- [ ] Получить статистику ошибок
- [ ] Определить топ-20 файлов с ошибками
- [ ] Создать backup ветку (на всякий случай)

### Во время исправления:
- [ ] Исправить автоматически исправляемые ошибки
- [ ] Исправить catch блоки
- [ ] Исправить неиспользуемые импорты (топ-20 файлов)
- [ ] Исправить типы any (топ-10 файлов)
- [ ] Исправить неэкранированные символы

### Перед коммитом:
- [ ] Очистить кэш ESLint
- [ ] Проверить локально: `pnpm lint --no-cache`
- [ ] Проверить TypeScript: `pnpm typecheck`
- [ ] Убедиться, что ошибок < 100 (или разбить на части)

---

## 🛠️ Полезные команды

### Анализ ошибок
```bash
# Статистика по типам
pnpm --filter web lint 2>&1 | grep -oE "@typescript-eslint/[^ ]+|react/[^ ]+" | sort | uniq -c | sort -rn

# Топ-20 файлов с ошибками
pnpm --filter web lint 2>&1 | grep -E "error" | cut -d: -f1 | sort | uniq -c | sort -rn | head -20

# Ошибки в конкретном файле
pnpm --filter web lint 2>&1 | grep "путь/к/файлу"
```

### Проверка локально (как в CI/CD)
```bash
# Очистить кэш
rm -rf .eslintcache apps/web/.eslintcache

# Проверить без кэша
pnpm --filter web lint --no-cache

# Проверить TypeScript
pnpm --filter web typecheck
```

### Массовое исправление
```bash
# Автоматическое исправление
pnpm --filter web lint --fix

# Исправить catch блоки
./apps/web/scripts/fix-catch-blocks.sh

# Исправить все (комплексный скрипт)
./apps/web/scripts/fix-all-lint.sh
```

---

## 🎯 Рекомендуемый алгоритм (минимум билдов)

### День 1: Подготовка (локально, без коммита)

1. **Анализ** (5 мин)
   ```bash
   pnpm --filter web lint 2>&1 | grep -E "error" | wc -l
   pnpm --filter web lint 2>&1 | grep -E "error" | cut -d: -f1 | sort | uniq -c | sort -rn | head -20 > top-20-files.txt
   ```

2. **Автоматическое исправление** (5 мин)
   ```bash
   pnpm --filter web lint --fix
   ```

3. **Исправление catch блоков** (5 мин)
   ```bash
   ./apps/web/scripts/fix-catch-blocks.sh
   ```

4. **Исправление топ-20 файлов** (30-60 мин)
   - Открыть `top-20-files.txt`
   - Исправить каждый файл
   - Проверять локально после каждого файла

5. **Проверка** (5 мин)
   ```bash
   rm -rf .eslintcache apps/web/.eslintcache
   pnpm --filter web lint --no-cache
   pnpm --filter web typecheck
   ```

### День 2: Коммит и проверка

1. **Если ошибок < 100** - один коммит
   ```bash
   git add -A
   git commit -m "fix: массовое исправление ESLint и TypeScript ошибок"
   git push
   ```

2. **Если ошибок > 100** - разбить на части
   - Коммит 1: Автоматические + catch блоки
   - Коммит 2: Топ-10 файлов
   - Коммит 3: Следующие 10 файлов
   - И так далее

---

## 💡 Советы для минимизации билдов

1. **Исправлять локально полностью** перед коммитом
2. **Использовать `--no-cache`** для проверки
3. **Коммитить большими блоками** (если уверены)
4. **Использовать feature-ветку** для тестирования
5. **Проверять локально как в CI/CD**

---

## 🔗 Связанные документы

- [Классификация ошибок](./error-types-classification.md)
- [Различия локальной и CI/CD среды](./local-vs-ci-errors-analysis.md)
- [Постоянное решение](./permanent-solution.md)


