# Оставшиеся задачи для полной реализации Local Indicators

## Статус реализации

### ✅ Уже реализовано:

1. **Парсеры для DOC.ua и Helsi** (`review-parsers.ts`)
   - ✅ Функции поиска URL клиники
   - ✅ Функции парсинга отзывов
   - ✅ Извлечение метрик (ответы, время ответа, негативные отзывы)

2. **Клиент Google Custom Search** (`google-custom-search-client.ts`)
   - ✅ Поиск упоминаний клиники
   - ✅ Фильтрация по локальным доменам
   - ✅ Проверка наличия бэклинков через Firecrawl

3. **База известных источников** (`known-sources.ts`)
   - ✅ Структура данных
   - ✅ Функции поиска источников по городу
   - ✅ Классификация источников

4. **Обновление analyzeReviewResponse()**
   - ✅ Интеграция парсеров DOC.ua и Helsi
   - ✅ Объединение результатов с Google Places API
   - ⚠️ НО: не передаются параметры `clinicName`, `city`, `firecrawlApiKey`

---

## ❌ Что нужно реализовать:

### 1. КРИТИЧНО: Реализовать analyzeLocalBacklinks()

**Текущее состояние:** Функция все еще возвращает пустые данные (placeholder)

**Что нужно сделать:**

```typescript
async function analyzeLocalBacklinks(
  domain: string,
  city?: string,
  clinicName?: string,
  clinicUrl?: string,
  googleCustomSearchApiKey?: string,
  googleCustomSearchEngineId?: string,
  firecrawlApiKey?: string,
): Promise<LocalBacklinks> {
  // 1. Поиск через Google Custom Search (если есть ключи)
  // 2. Проверка известных источников через Firecrawl
  // 3. Проверка наличия ссылок на найденных страницах
  // 4. Классификация и дедупликация
  // 5. Возврат структурированных данных
}
```

**Задачи:**
- [ ] Добавить параметры в функцию `analyzeLocalBacklinks()`
- [ ] Реализовать поиск через Google Custom Search API
- [ ] Реализовать проверку известных источников
- [ ] Реализовать проверку наличия ссылок через `checkBacklink()`
- [ ] Реализовать классификацию источников
- [ ] Реализовать дедупликацию по доменам
- [ ] Подсчитать метрики по типам источников

**Файл:** `apps/web/lib/server/services/local/local-analyzer.ts` (строки 608-652)

---

### 2. КРИТИЧНО: Обновить analyzeLocalIndicators()

**Текущее состояние:** Не передает новые параметры в функции анализа

**Что нужно сделать:**

```typescript
export async function analyzeLocalIndicators(
  url: string,
  placeId?: string,
  googleApiKey?: string,
  city?: string,
  clinicName?: string, // НОВЫЙ ПАРАМЕТР
  firecrawlApiKey?: string, // НОВЫЙ ПАРАМЕТР
  googleCustomSearchApiKey?: string, // НОВЫЙ ПАРАМЕТР
  googleCustomSearchEngineId?: string, // НОВЫЙ ПАРАМЕТР
): Promise<LocalIndicatorsAuditResult> {
  // ...
  
  // Обновить вызовы:
  analyzeReviewResponse(
    placeId, 
    googleApiKey, 
    clinicName, // ДОБАВИТЬ
    city, // ДОБАВИТЬ
    firecrawlApiKey // ДОБАВИТЬ
  ),
  
  analyzeLocalBacklinks(
    getDomain(url) || '', 
    city,
    clinicName, // ДОБАВИТЬ
    url, // ДОБАВИТЬ
    googleCustomSearchApiKey, // ДОБАВИТЬ
    googleCustomSearchEngineId, // ДОБАВИТЬ
    firecrawlApiKey // ДОБАВИТЬ
  ),
}
```

**Задачи:**
- [ ] Добавить новые параметры в сигнатуру функции
- [ ] Обновить вызов `analyzeReviewResponse()` с новыми параметрами
- [ ] Обновить вызов `analyzeLocalBacklinks()` с новыми параметрами
- [ ] Извлечь `clinicName` из schema или передать как параметр

**Файл:** `apps/web/lib/server/services/local/local-analyzer.ts` (строки 992-1095)

---

### 3. КРИТИЧНО: Обновить local-indicators-audit.ts

**Текущее состояние:** Не принимает и не передает новые параметры

**Что нужно сделать:**

```typescript
const LocalIndicatorsInputSchema = z.object({
  url: z.string().url('Please provide a valid URL'),
  placeId: z.string().optional(),
  googleApiKey: z.string().optional(),
  city: z.string().optional(),
  // ДОБАВИТЬ:
  clinicName: z.string().optional(), // Название клиники для поиска на DOC.ua/Helsi
  firecrawlApiKey: z.string().optional(), // Firecrawl API ключ
  googleCustomSearchApiKey: z.string().optional(), // Google Custom Search API ключ
  googleCustomSearchEngineId: z.string().optional(), // Google Custom Search Engine ID
});

// В функции performLocalIndicatorsAudit:
const result = await analyzeLocalIndicators(
  normalizedUrl,
  placeId,
  apiKey,
  city,
  input.clinicName, // ДОБАВИТЬ
  input.firecrawlApiKey || process.env.FIRECRAWL_API_KEY, // ДОБАВИТЬ
  input.googleCustomSearchApiKey || process.env.GOOGLE_CUSTOM_SEARCH_API_KEY, // ДОБАВИТЬ
  input.googleCustomSearchEngineId || process.env.GOOGLE_CUSTOM_SEARCH_ENGINE_ID, // ДОБАВИТЬ
);
```

**Задачи:**
- [ ] Добавить новые поля в `LocalIndicatorsInputSchema`
- [ ] Обновить вызов `analyzeLocalIndicators()` с новыми параметрами
- [ ] Добавить fallback на переменные окружения

**Файл:** `apps/web/lib/actions/local-indicators-audit.ts`

---

### 4. ВАЖНО: Обновить UI (форма ввода)

**Текущее состояние:** Форма не имеет полей для новых параметров

**Что нужно сделать:**

Добавить поля в форму на странице `apps/web/app/[locale]/home/local-indicators/page.tsx`:

```tsx
<FormField
  name="clinicName"
  label="Clinic Name (optional)"
  description="For searching reviews on DOC.ua and Helsi"
/>

<FormField
  name="firecrawlApiKey"
  label="Firecrawl API Key (optional)"
  description="For parsing DOC.ua, Helsi, and checking backlinks"
/>

<FormField
  name="googleCustomSearchApiKey"
  label="Google Custom Search API Key (optional)"
  description="For finding local backlinks"
/>

<FormField
  name="googleCustomSearchEngineId"
  label="Google Custom Search Engine ID (optional)"
  description="Your Custom Search Engine ID"
/>
```

**Задачи:**
- [ ] Добавить поля в форму
- [ ] Добавить валидацию (опциональные поля)
- [ ] Сохранять значения в localStorage (как другие API ключи)
- [ ] Добавить подсказки/описания для каждого поля

**Файл:** `apps/web/app/[locale]/home/local-indicators/page.tsx`

---

### 5. ВАЖНО: Исправить дубликат импорта

**Проблема:** В `local-analyzer.ts` есть дубликат импорта `fetchDocUaReviews, fetchHelsiReviews` (строки 37 и 47)

**Задачи:**
- [ ] Удалить дубликат импорта

**Файл:** `apps/web/lib/server/services/local/local-analyzer.ts` (строка 47)

---

### 6. РЕКОМЕНДУЕТСЯ: Улучшить парсеры DOC.ua/Helsi

**Текущее состояние:** Базовые парсеры, могут не работать с реальной структурой HTML

**Что нужно сделать:**

- [ ] Протестировать на реальных страницах DOC.ua
- [ ] Протестировать на реальных страницах Helsi
- [ ] Адаптировать селекторы под реальную структуру HTML
- [ ] Добавить fallback методы парсинга
- [ ] Улучшить обработку ошибок

**Файл:** `apps/web/lib/server/services/local/review-parsers.ts`

---

### 7. РЕКОМЕНДУЕТСЯ: Расширить базу известных источников

**Текущее состояние:** Базовая база с несколькими источниками

**Что нужно сделать:**

- [ ] Добавить больше источников для каждого города
- [ ] Добавить источники для других крупных городов Украины
- [ ] Добавить медицинские ассоциации
- [ ] Добавить благотворительные фонды
- [ ] Создать JSON файл для легкого расширения

**Файл:** `apps/web/lib/server/services/local/known-sources.ts`

---

### 8. РЕКОМЕНДУЕТСЯ: Добавить кэширование

**Текущее состояние:** Нет кэширования результатов

**Что нужно сделать:**

- [ ] Кэшировать результаты Google Custom Search (24 часа)
- [ ] Кэшировать результаты проверки известных источников (24 часа)
- [ ] Кэшировать результаты парсинга DOC.ua/Helsi (12 часов)
- [ ] Использовать Redis или in-memory cache

**Файлы:**
- `apps/web/lib/server/services/local/local-analyzer.ts`
- `apps/web/lib/server/services/local/google-custom-search-client.ts`

---

### 9. РЕКОМЕНДУЕТСЯ: Добавить обработку rate limits

**Текущее состояние:** Нет обработки rate limits

**Что нужно сделать:**

- [ ] Отслеживать количество запросов к Google Custom Search API
- [ ] Обрабатывать ошибку 429 (Too Many Requests)
- [ ] Добавить очередь запросов
- [ ] Логировать предупреждения при приближении к лимиту

**Файл:** `apps/web/lib/server/services/local/google-custom-search-client.ts`

---

### 10. ОПЦИОНАЛЬНО: Добавить Google Search Console интеграцию

**Текущее состояние:** Не реализовано

**Что нужно сделать:**

- [ ] Реализовать OAuth 2.0 для Google Search Console
- [ ] Получать данные о бэклинках из Search Console
- [ ] Объединять с результатами Google Custom Search
- [ ] Сделать опциональным (требует OAuth)

**Приоритет:** Низкий (можно добавить позже)

---

## Приоритеты реализации

### 🔴 КРИТИЧНО (блокирует работу):
1. ✅ Реализовать `analyzeLocalBacklinks()` - **ОБЯЗАТЕЛЬНО**
2. ✅ Обновить `analyzeLocalIndicators()` - **ОБЯЗАТЕЛЬНО**
3. ✅ Обновить `local-indicators-audit.ts` - **ОБЯЗАТЕЛЬНО**
4. ✅ Обновить UI форму - **ОБЯЗАТЕЛЬНО**
5. ✅ Исправить дубликат импорта - **ОБЯЗАТЕЛЬНО**

### 🟡 ВАЖНО (улучшает качество):
6. Улучшить парсеры DOC.ua/Helsi
7. Расширить базу известных источников

### 🟢 РЕКОМЕНДУЕТСЯ (оптимизация):
8. Добавить кэширование
9. Добавить обработку rate limits
10. Google Search Console интеграция (опционально)

---

## Оценка времени

- **Критичные задачи:** 4-6 часов
- **Важные задачи:** 4-6 часов
- **Рекомендуемые задачи:** 6-8 часов
- **Итого:** 14-20 часов (2-3 рабочих дня)

---

## Чеклист для проверки

После реализации проверить:

- [ ] `analyzeLocalBacklinks()` возвращает реальные данные
- [ ] DOC.ua отзывы парсятся корректно
- [ ] Helsi отзывы парсятся корректно
- [ ] Google Custom Search находит упоминания
- [ ] Проверка известных источников работает
- [ ] Бэклинки классифицируются правильно
- [ ] Дедупликация работает
- [ ] UI форма принимает все параметры
- [ ] Ошибки обрабатываются корректно
- [ ] Логирование работает

---

## Заключение

Основная работа уже сделана (парсеры, клиенты, база источников). Осталось:
1. Интегрировать все компоненты в `analyzeLocalBacklinks()`
2. Обновить передачу параметров через всю цепочку
3. Обновить UI для ввода новых параметров

После этого система будет полностью функциональна согласно ТЗ.




