# План полной реализации E-E-A-T Audit Domain

## Обзор
Детальный план реализации всех требований спецификации (пункты 5.1-5.11) для E-E-A-T audit domain.

---

## Архитектурные изменения

### 1. Мультистраничный анализ
**Проблема:** Текущая реализация анализирует только одну страницу, но спецификация требует анализа множества страниц (например, "% страниц блога с автором").

**Решение:**
- Добавить функцию `analyzeMultiplePages(urls: string[])` для анализа списка URL
- Добавить функцию `discoverPages(baseUrl: string)` для автоматического обнаружения страниц через:
  - Sitemap.xml
  - Robots.txt
  - Внутренние ссылки (crawling)
- Добавить агрегацию результатов по множеству страниц

**Файлы для изменения:**
- `src/server/services/eeat/eeat-analyzer.ts` - добавить функции мультистраничного анализа
- `src/server/services/eeat/page-discovery.ts` - новый файл для обнаружения страниц
- `src/server/services/eeat/types.ts` - обновить схемы для агрегированных результатов

---

## Детальный план по пунктам

### 📋 5.1. Authors (Авторы)

#### Текущее состояние:
- ✅ `has_author_blocks` - проверяет ссылки на `/doctors/`, `/team/`
- ✅ `author_credentials_found` - ищет паттерны "Dr.", "к.м.н.", "MD"

#### Что нужно добавить:

**Задача 1.1: Проверка автора на каждой статье**
- Функция: `checkArticleAuthor($: CheerioAPI, articleUrl: string): ArticleAuthorInfo`
- Логика:
  - Определить, является ли страница статьей (проверка структуры: `<article>`, классы "blog", "post", "article")
  - Проверить наличие блока автора (селекторы: `.author`, `.byline`, `[itemprop="author"]`)
  - Извлечь имя автора
  - Проверить наличие ссылки на профиль автора
- Схема: добавить `ArticleAuthorInfo` в `types.ts`

**Задача 1.2: Проверка профиля автора**
- Функция: `analyzeAuthorProfile($: CheerioAPI): AuthorProfileInfo`
- Логика:
  - Проверить наличие квалификации (Dr., MD, к.м.н., PhD)
  - Проверить наличие позиции/должности (Cardiologist, Surgeon, etc.)
  - Проверить наличие опыта (годы работы)
  - Проверить наличие ссылки на дипломы/сертификаты
- Схема: добавить `AuthorProfileInfo` в `types.ts`

**Задача 1.3: Агрегация по множеству страниц**
- Функция: `calculateAuthorMetrics(articles: ArticleAuthorInfo[]): AuthorMetrics`
- Расчет:
  - `blog_pages_with_author_percent` = (страницы с автором) / (все страницы блога) × 100
  - `authors_with_credentials_percent` = (авторы с квалификациями) / (все авторы) × 100
- Схема: добавить `AuthorMetrics` в `types.ts`

**Файлы:**
- `src/server/services/eeat/eeat-analyzer.ts` - добавить функции
- `src/server/services/eeat/types.ts` - обновить схемы
- `src/server/services/eeat/article-detector.ts` - новый файл для детекции статей

---

### 📋 5.2. Doctor Expertise (Экспертиза врачей)

#### Текущее состояние:
- ✅ `author_credentials_found` - ищет паттерны "Dr.", "к.м.н.", "MD"

#### Что нужно добавить:

**Задача 2.1: Проверка дипломов и сертификатов**
- Функция: `checkDoctorCredentials($: CheerioAPI): DoctorCredentialsInfo`
- Логика:
  - Искать ссылки на документы с паттернами:
    - `/diploma`, `/certificate`, `/license`
    - Изображения с alt/text содержащими "диплом", "сертификат", "certificate", "diploma"
    - PDF файлы с соответствующими названиями
  - Проверить наличие текстовых упоминаний дипломов/сертификатов
- Схема: добавить `DoctorCredentialsInfo` в `types.ts`

**Задача 2.2: Проверка членства в ассоциациях**
- Функция: `checkAssociationMembership($: CheerioAPI): AssociationInfo[]`
- Логика:
  - Искать текстовые упоминания: "асоціація", "association", "член", "member"
  - Искать ссылки на сайты ассоциаций (медицинские организации)
  - Проверить наличие логотипов/упоминаний известных ассоциаций
- Схема: добавить `AssociationInfo` в `types.ts`

**Задача 2.3: Проверка курсов повышения квалификации**
- Функция: `checkContinuingEducation($: CheerioAPI): EducationInfo[]`
- Логика:
  - Искать текстовые упоминания: "курс", "course", "навчання", "training", "сертифікація"
  - Искать ссылки на сертификаты курсов
  - Проверить наличие дат прохождения курсов
- Схема: добавить `EducationInfo` в `types.ts`

**Задача 2.4: Агрегация по страницам врачей**
- Функция: `calculateDoctorExpertiseMetrics(doctorPages: DoctorCredentialsInfo[]): DoctorExpertiseMetrics`
- Расчет:
  - `doctor_pages_with_credentials_percent` = (страницы с квалификациями) / (все страницы врачей) × 100
- Схема: добавить `DoctorExpertiseMetrics` в `types.ts`

**Файлы:**
- `src/server/services/eeat/eeat-analyzer.ts` - добавить функции
- `src/server/services/eeat/types.ts` - обновить схемы
- `src/server/services/eeat/doctor-analyzer.ts` - новый файл для анализа страниц врачей

---

### 📋 5.3. Clinic Experience (Опыт клиники)

#### Текущее состояние:
- ✅ `has_case_studies` - проверяет наличие ссылок на case studies
- ✅ `experience_figures_found` - ищет паттерны "10+ років", "5000+ пацієнтів"

#### Что нужно добавить:

**Задача 3.1: Проверка структуры case study**
- Функция: `analyzeCaseStudyStructure($: CheerioAPI, caseStudyUrl: string): CaseStudyStructure`
- Логика:
  - Проверить наличие секций:
    - Жалоба (complaint, symptoms, проблема)
    - Диагноз (diagnosis, діагноз)
    - Лечение (treatment, лікування, therapy)
    - Результат (result, результат, outcome)
  - Проверить наличие временных рамок (timeline, дати)
  - Проверить наличие метрик (до/после, показатели)
  - Проверить наличие комментариев врача (doctor comment, commentary)
- Схема: добавить `CaseStudyStructure` в `types.ts`

**Задача 3.2: Проверка специальностей в case studies**
- Функция: `checkCaseStudiesBySpecialty($: CheerioAPI): CaseStudiesBySpecialty`
- Логика:
  - Определить специальности: Cardiology, Dentistry, Ophthalmology, etc.
  - Подсчитать количество case studies по каждой специальности
  - Проверить наличие case studies для основных специальностей клиники
- Схема: добавить `CaseStudiesBySpecialty` в `types.ts`

**Задача 3.3: Проверка отсутствия PII данных**
- Функция: `checkPIICompliance($: CheerioAPI): boolean`
- Логика:
  - Проверить отсутствие полных имен пациентов
  - Проверить отсутствие адресов пациентов
  - Проверить отсутствие телефонных номеров пациентов
  - Проверить наличие анонимизации (инициалы, "Пацієнт N")
- Схема: добавить `PIIComplianceInfo` в `types.ts`

**Файлы:**
- `src/server/services/eeat/eeat-analyzer.ts` - добавить функции
- `src/server/services/eeat/types.ts` - обновить схемы
- `src/server/services/eeat/case-study-analyzer.ts` - новый файл для анализа case studies

---

### 📋 5.4. Reputation (Репутация)

#### Текущее состояние:
- ✅ `linked_platforms` - детектирует наличие ссылок на Google Maps, Doc.ua, Likarni
- ✅ `social_links` - детектирует ссылки на Facebook, Instagram, YouTube

#### Что нужно добавить:

**Задача 4.1: Интеграция с Google Maps API (опционально)**
- Функция: `fetchGoogleMapsRating(placeId: string, apiKey: string): GoogleMapsRating`
- Логика:
  - Использовать Google Places API для получения:
    - Среднего рейтинга (rating)
    - Количества отзывов (user_ratings_total)
    - Последних отзывов (reviews)
  - Обработать ошибки (API ключ не предоставлен)
- Схема: добавить `GoogleMapsRating` в `types.ts`
- Зависимости: добавить `@googlemaps/google-maps-services-js` или использовать fetch

**Задача 4.2: Интеграция с агрегаторами (опционально)**
- Функция: `fetchAggregatorRatings(clinicName: string, city: string): AggregatorRatings`
- Логика:
  - Для Doc.ua: парсинг страницы клиники (если доступна)
  - Для Likarni.com: парсинг страницы клиники (если доступна)
  - Извлечение рейтинга и количества отзывов
  - Обработка случая, когда клиника не найдена (не снижает средний балл)
- Схема: добавить `AggregatorRatings` в `types.ts`

**Задача 4.3: Анализ сентимента отзывов**
- Функция: `analyzeReviewSentiment(reviews: Review[]): SentimentAnalysis`
- Логика:
  - Простой анализ ключевых слов (положительные/отрицательные)
  - Подсчет процента положительных отзывов
  - Детекция агрессивных ответов на жалобы
- Схема: добавить `SentimentAnalysis` в `types.ts`
- Примечание: Для полноценного анализа может потребоваться NLP/ML (опционально)

**Задача 4.4: Расчет среднего рейтинга**
- Функция: `calculateAverageRating(ratings: PlatformRating[]): AverageRating`
- Логика:
  - Взвешенное среднее по платформам (с учетом количества отзывов)
  - Исключить платформы, где клиника не найдена
- Схема: добавить `AverageRating` в `types.ts`

**Файлы:**
- `src/server/services/eeat/eeat-analyzer.ts` - добавить функции
- `src/server/services/eeat/types.ts` - обновить схемы
- `src/server/services/eeat/reputation-analyzer.ts` - новый файл для анализа репутации
- `src/server/services/eeat/google-maps-client.ts` - новый файл для Google Maps API
- `src/server/services/eeat/aggregator-parser.ts` - новый файл для парсинга агрегаторов

---

### 📋 5.5. Patient Stories (Case Studies)

#### Текущее состояние:
- ✅ `has_case_studies` - проверяет наличие ссылок на case studies

#### Что нужно добавить:

**Задача 5.1: Проверка структурированного шаблона**
- Функция: `checkCaseStudyTemplate($: CheerioAPI): CaseStudyTemplate`
- Логика:
  - Проверить наличие всех секций:
    - Жалоба (complaint, symptoms)
    - Диагноз (diagnosis)
    - Лечение (treatment)
    - Результат (result)
    - Временные рамки (timeline)
  - Проверить, что это не просто отзыв "они мне помогли"
  - Оценить полноту структуры (0-100%)
- Схема: добавить `CaseStudyTemplate` в `types.ts`

**Задача 5.2: Проверка специальностей**
- Функция: `checkCaseStudiesBySpecialty($: CheerioAPI): CaseStudiesBySpecialty`
- Логика:
  - Определить специальности из case studies
  - Подсчитать количество по каждой специальности
  - Проверить наличие для основных специальностей
- Схема: добавить `CaseStudiesBySpecialty` в `types.ts`

**Задача 5.3: Проверка комментариев врача**
- Функция: `checkDoctorCommentary($: CheerioAPI): boolean`
- Логика:
  - Искать блоки с комментариями врача (селекторы: `.doctor-comment`, `.expert-opinion`)
  - Проверить наличие объяснения выбора тактики лечения
- Схема: добавить в `CaseStudyTemplate`

**Файлы:**
- `src/server/services/eeat/eeat-analyzer.ts` - добавить функции
- `src/server/services/eeat/types.ts` - обновить схемы
- `src/server/services/eeat/case-study-analyzer.ts` - использовать существующий файл

---

### 📋 5.6. GEO + NAP (Геолокация и NAP)

#### Текущее состояние:
- ✅ `nap_present` - проверяет наличие телефона (tel:) и адреса

#### Что нужно добавить:

**Задача 6.1: Извлечение NAP данных с сайта**
- Функция: `extractNAPFromWebsite($: CheerioAPI): NAPData`
- Логика:
  - Извлечь Name (название клиники) из:
    - `<title>`, `<h1>`, `[itemprop="name"]`
  - Извлечь Address из:
    - `[itemprop="address"]`, `.address`, текстовые паттерны
  - Извлечь Phone из:
    - `tel:` ссылки, `[itemprop="telephone"]`
- Схема: добавить `NAPData` в `types.ts`

**Задача 6.2: Интеграция с Google Business Profile API (опционально)**
- Функция: `fetchGoogleBusinessNAP(placeId: string, apiKey: string): NAPData`
- Логика:
  - Использовать Google My Business API для получения NAP
  - Обработать ошибки (API ключ не предоставлен)
- Схема: использовать `NAPData`

**Задача 6.3: Сравнение NAP данных**
- Функция: `compareNAP(websiteNAP: NAPData, googleNAP: NAPData, aggregatorNAP: NAPData[]): NAPComparison`
- Логика:
  - Нормализовать данные (убрать пробелы, привести к единому формату)
  - Сравнить Name (точное совпадение или fuzzy matching)
  - Сравнить Address (нормализация адресов)
  - Сравнить Phone (нормализация номеров)
  - Рассчитать процент совпадений
- Схема: добавить `NAPComparison` в `types.ts`

**Задача 6.4: Детекция дубликатов**
- Функция: `detectDuplicateListings(placeIds: string[]): DuplicateInfo[]`
- Логика:
  - Проверить наличие нескольких листингов для одной клиники
  - Использовать Google Places API для проверки дубликатов
- Схема: добавить `DuplicateInfo` в `types.ts`

**Файлы:**
- `src/server/services/eeat/eeat-analyzer.ts` - добавить функции
- `src/server/services/eeat/types.ts` - обновить схемы
- `src/server/services/eeat/nap-analyzer.ts` - новый файл для анализа NAP
- `src/server/services/eeat/google-business-client.ts` - новый файл для Google Business API

---

### 📋 5.7. Transparency of Contacts and Ownership

#### Текущее состояние:
- ✅ `contact_page_found` - проверяет наличие ссылки на `/contact` или `tel:`
- ✅ `nap_present` - проверяет наличие адреса и телефона

#### Что нужно добавить:

**Задача 7.1: Проверка юридического лица**
- Функция: `checkLegalEntity($: CheerioAPI): LegalEntityInfo`
- Логика:
  - Искать название юридического лица:
    - Текст "ТОВ", "ПП", "ФОП", "LLC", "Ltd"
    - Секция "Про нас" / "About Us"
    - Footer с юридической информацией
  - Проверить наличие регистрационных данных (ЕДРПОУ, налоговый номер)
- Схема: добавить `LegalEntityInfo` в `types.ts`

**Задача 7.2: Проверка страницы "О клинике"**
- Функция: `checkAboutUsPage($: CheerioAPI, baseUrl: string): AboutUsInfo`
- Логика:
  - Проверить наличие ссылки на `/about`, `/about-us`, `/про-нас`
  - Проверить содержание страницы (если доступна):
    - История клиники
    - Миссия/ценности
    - Команда
- Схема: добавить `AboutUsInfo` в `types.ts`

**Задача 7.3: Проверка полного блока контактов**
- Функция: `checkContactBlock($: CheerioAPI): ContactBlockInfo`
- Логика:
  - Проверить наличие всех элементов:
    - Адрес (уже есть в NAP)
    - Телефон (уже есть в NAP)
    - Email (`mailto:` ссылки)
    - Форма бронирования (form с полями для записи)
    - Карта (Google Maps embed или iframe)
- Схема: добавить `ContactBlockInfo` в `types.ts`

**Задача 7.4: Проверка отделов клиники**
- Функция: `checkDepartments($: CheerioAPI): DepartmentInfo[]`
- Логика:
  - Искать навигацию с отделами (меню "Послуги", "Відділення")
  - Искать ссылки на страницы отделов
  - Извлечь список отделов
- Схема: добавить `DepartmentInfo` в `types.ts`

**Файлы:**
- `src/server/services/eeat/eeat-analyzer.ts` - добавить функции
- `src/server/services/eeat/types.ts` - обновить схемы
- `src/server/services/eeat/transparency-analyzer.ts` - новый файл для анализа прозрачности

---

### 📋 5.8. Licenses and Accreditations

#### Текущее состояние:
- ✅ `has_licenses` - ищет паттерны "Ліцензія", "License", "Наказ МОЗ"

#### Что нужно добавить:

**Задача 8.1: Проверка изображений лицензий**
- Функция: `checkLicenseImages($: CheerioAPI): LicenseImageInfo[]`
- Логика:
  - Искать изображения с alt/text содержащими "ліцензія", "license"
  - Искать ссылки на PDF/изображения с паттернами:
    - `/license`, `/licenses`, `/ліцензія`
    - Файлы с расширениями `.pdf`, `.jpg`, `.png`
- Схема: добавить `LicenseImageInfo` в `types.ts`

**Задача 8.2: Проверка отдельного раздела с лицензиями**
- Функция: `checkLicenseSection($: CheerioAPI): LicenseSectionInfo`
- Логика:
  - Проверить наличие ссылки на `/licenses`, `/ліцензії`, `/accreditations`
  - Проверить наличие секции на странице с лицензиями
  - Проверить структуру раздела (список лицензий, даты, номера)
- Схема: добавить `LicenseSectionInfo` в `types.ts`

**Задача 8.3: Проверка аккредитаций**
- Функция: `checkAccreditations($: CheerioAPI): AccreditationInfo[]`
- Логика:
  - Искать текстовые упоминания: "акредитація", "accreditation"
  - Искать ссылки на документы аккредитации
  - Отличить аккредитации от лицензий
- Схема: добавить `AccreditationInfo` в `types.ts`

**Файлы:**
- `src/server/services/eeat/eeat-analyzer.ts` - добавить функции
- `src/server/services/eeat/types.ts` - обновить схемы
- `src/server/services/eeat/license-analyzer.ts` - новый файл для анализа лицензий

---

### 📋 5.9. Scientific Sources (Научные источники)

#### Текущее состояние:
- ✅ `scientific_sources_count` - подсчитывает ссылки на научные домены

#### Что нужно добавить:

**Задача 9.1: Расчет процента статей со ссылками**
- Функция: `calculateScientificSourcesMetrics(articles: ArticleInfo[]): ScientificSourcesMetrics`
- Логика:
  - Для каждой статьи проверить наличие ссылок на научные источники
  - Рассчитать: `articles_with_sources_percent` = (статьи со ссылками) / (все статьи) × 100
- Схема: добавить `ScientificSourcesMetrics` в `types.ts`

**Задача 9.2: Проверка национальных протоколов**
- Функция: `checkNationalProtocols($: CheerioAPI): ProtocolInfo[]`
- Логика:
  - Искать ссылки на moz.gov.ua (уже есть)
  - Искать ссылки на другие национальные источники
  - Проверить упоминания протоколов в тексте
- Схема: добавить `ProtocolInfo` в `types.ts`

**Задача 9.3: Проверка международных протоколов**
- Функция: `checkInternationalProtocols($: CheerioAPI): ProtocolInfo[]`
- Логика:
  - Искать ссылки на WHO (уже есть)
  - Искать ссылки на другие международные организации
  - Проверить упоминания международных протоколов
- Схема: использовать `ProtocolInfo`

**Файлы:**
- `src/server/services/eeat/eeat-analyzer.ts` - добавить функции
- `src/server/services/eeat/types.ts` - обновить схемы
- `src/server/services/eeat/scientific-sources-analyzer.ts` - новый файл для анализа научных источников

---

### 📋 5.10. Privacy (Конфиденциальность)

#### Текущее состояние:
- ✅ `has_privacy_policy` - проверяет наличие ссылок на Privacy Policy

#### Что нужно добавить:

**Задача 10.1: Проверка содержания политики конфиденциальности**
- Функция: `analyzePrivacyPolicyContent($: CheerioAPI, privacyUrl: string): PrivacyPolicyContent`
- Логика:
  - Загрузить страницу Privacy Policy (если доступна)
  - Проверить наличие секций:
    - Описание обработки медицинских данных
    - Согласие на обработку (GDPR consent)
    - Права пользователя
    - Контакты для вопросов
- Схема: добавить `PrivacyPolicyContent` в `types.ts`

**Задача 10.2: Проверка GDPR compliance**
- Функция: `checkGDPRCompliance($: CheerioAPI): GDPRComplianceInfo`
- Логика:
  - Проверить наличие cookie banner
  - Проверить наличие формы согласия на обработку данных
  - Проверить наличие ссылки на Privacy Policy в форме
- Схема: добавить `GDPRComplianceInfo` в `types.ts`

**Файлы:**
- `src/server/services/eeat/eeat-analyzer.ts` - добавить функции
- `src/server/services/eeat/types.ts` - обновить схемы
- `src/server/services/eeat/privacy-analyzer.ts` - новый файл для анализа Privacy

---

### 📋 5.11. Community Interaction (Взаимодействие с сообществом)

#### Текущее состояние:
- ✅ `has_community_mentions` - ищет ключевые слова о конференциях, выступлениях

#### Что нужно добавить:

**Задача 11.1: Проверка исходящих ссылок на СМИ**
- Функция: `checkMediaLinks($: CheerioAPI): MediaLinkInfo[]`
- Логика:
  - Искать исходящие ссылки на медицинские СМИ
  - Искать ссылки на статьи/публикации о клинике/врачах
  - Проверить авторитетность источников (медицинские издания)
- Схема: добавить `MediaLinkInfo` в `types.ts`

**Задача 11.2: Проверка публикаций в журналах**
- Функция: `checkJournalPublications($: CheerioAPI): PublicationInfo[]`
- Логика:
  - Искать ссылки на научные журналы
  - Искать упоминания публикаций в тексте
  - Проверить наличие DOI или ссылок на статьи
- Схема: добавить `PublicationInfo` в `types.ts`

**Задача 11.3: Проверка членства в ассоциациях (расширенная)**
- Функция: `checkAssociationMembershipDetailed($: CheerioAPI): AssociationMembershipInfo[]`
- Логика:
  - Искать ссылки на сайты ассоциаций
  - Искать логотипы известных ассоциаций
  - Проверить наличие подтверждения членства
- Схема: добавить `AssociationMembershipInfo` в `types.ts`

**Файлы:**
- `src/server/services/eeat/eeat-analyzer.ts` - добавить функции
- `src/server/services/eeat/types.ts` - обновить схемы
- `src/server/services/eeat/community-analyzer.ts` - новый файл для анализа сообщества

---

## Общие задачи

### Задача G1: Обновление схем данных
**Файл:** `src/server/services/eeat/types.ts`

**Действия:**
- Добавить все новые схемы из плана
- Обновить `EEATAuditResultSchema` для включения новых полей
- Добавить опциональные поля для API-зависимых данных

### Задача G2: Обновление UI компонента
**Файл:** `src/components/features/playground/EEATAuditSection.tsx`

**Действия:**
- Добавить отображение новых метрик
- Добавить карточки для новых разделов
- Добавить индикаторы для опциональных данных (требуют API ключи)

### Задача G3: Обновление Server Action
**Файл:** `src/server/actions/eeat-audit.ts`

**Действия:**
- Добавить поддержку мультистраничного анализа
- Добавить опциональные параметры для API ключей
- Добавить обработку ошибок для внешних API

### Задача G4: Добавление конфигурации
**Файл:** `src/server/services/eeat/config.ts` (новый)

**Действия:**
- Добавить конфигурацию для API ключей (Google Maps, Google Business)
- Добавить настройки для опциональных функций
- Добавить список известных медицинских ассоциаций

---

## Приоритизация

### Фаза 1: Критичные улучшения (2-3 недели)
1. ✅ Мультистраничный анализ (G1, 5.1, 5.2, 5.3)
2. ✅ Улучшение проверки структуры (5.3, 5.5)
3. ✅ Проверка документов (5.2, 5.8)
4. ✅ Улучшение прозрачности (5.7)

### Фаза 2: Важные улучшения (2-3 недели)
5. ✅ Проверка исходящих ссылок (5.11)
6. ✅ Улучшение NAP анализа (5.6)
7. ✅ Улучшение Privacy анализа (5.10)
8. ✅ Улучшение Scientific Sources (5.9)

### Фаза 3: Опциональные улучшения (1-2 недели)
9. ⚠️ Интеграция с Google Maps API (5.4) - требует API ключ
10. ⚠️ Интеграция с Google Business API (5.6) - требует API ключ
11. ⚠️ Анализ сентимента отзывов (5.4) - опционально

---

## Оценка времени

- **Фаза 1:** 2-3 недели (80-120 часов)
- **Фаза 2:** 2-3 недели (80-120 часов)
- **Фаза 3:** 1-2 недели (40-80 часов)

**Итого:** 5-8 недель (200-320 часов)

---

## Зависимости

### Внешние библиотеки:
- `cheerio` - уже используется
- `@googlemaps/google-maps-services-js` - для Google Maps API (опционально)
- `zod` - уже используется

### API ключи (опционально):
- Google Maps API key
- Google My Business API key

---

## Тестирование

### Unit тесты:
- Тесты для каждой функции детекции
- Тесты для агрегации результатов
- Тесты для схем валидации

### Integration тесты:
- Тесты для мультистраничного анализа
- Тесты для внешних API (с моками)

### E2E тесты:
- Тесты для полного flow E-E-A-T audit
- Тесты для UI компонента

---

## Документация

1. Обновить README с описанием новых функций
2. Добавить примеры использования API
3. Добавить документацию по конфигурации
4. Добавить troubleshooting guide

---

## Заключение

Этот план обеспечивает полную реализацию всех требований спецификации E-E-A-T audit domain. Реализация разбита на фазы с четкими приоритетами и оценками времени.

